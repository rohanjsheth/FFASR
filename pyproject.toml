[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "ffasr"
version = "0.1.0"
description = "Synthetic far-field ASR scene generation and data preparation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "accelerate>=1.12,<2",
    "datasets>=5.0,<6",
    "matplotlib>=3.10,<4",
    "numpy>=2.0,<3",
    "scipy>=1.15,<2",
    "soundfile>=0.13,<1",
    "torch>=2.6,<3",
    "transformers>=5.13,<6",
]

[project.scripts]
ffasr-render-samples = "scripts.render_samples:main"
ffasr-smoke-test = "scripts.smoke_test_dataloader:main"

[tool.setuptools]
py-modules = ["SceneDataset", "data_collator", "data_utils"]

[tool.setuptools.packages.find]
include = ["audio_utils*", "scripts*"]
